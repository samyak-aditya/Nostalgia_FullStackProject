import { makeStyles, theme } from '@mui/styles';

export default makeStyles((theme) => ({
  mainContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  smMargin: {
  smMargin: '8px' ,
  },
  actionDiv: {
    textAlign: 'center',
  },
}));
